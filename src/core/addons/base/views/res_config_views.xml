<?xml version="1.0" encoding="utf-8"?>
<verp>
    <data>
        
        <record id="resConfigViewBase" model="ir.ui.view">
            <field name="label">res.config.view.base</field>
            <field name="model">res.config</field>
            <field name="arch" type="xml">
                <form string="Configuration">
                    <group name="resConfigContents"/>
                    <footer>
                        <button name="actionNext" type="object" string="Apply" class="btn-primary" data-hotkey="q"/>
                        <button name="actionSkip" type="object" special="cancel" data-hotkey="z" string="Cancel" class="btn-secondary"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="resConfigInstaller" model="ir.ui.view">
            <field name="label">Inheritable view for installer objects</field>
            <field name="model">res.config.installer</field>
            <field name="arch" type="xml">
                <form string="Configuration Installer">
                    <separator string="title" colspan="4"/>
                    <footer>
                        <button name="actionNext" type="object" string="Install Apps" class="btn-primary" data-hotkey="q"/>
                        <button string="Skip" class="btn-secondary" special="cancel" data-hotkey="z"/>
                    </footer>
                </form>
            </field>
        </record>

    </data>
</verp>
